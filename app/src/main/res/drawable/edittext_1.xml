<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Background color -->
    <solid android:color="@color/editbox"/>
    <corners android:radius="5dp"/>
    <!-- Border color and width -->
    <stroke
        android:color="@color/editbox"
        android:width="2dp"/>
</shape>